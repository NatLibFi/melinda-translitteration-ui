pipeline:
  test:
    image: node:8
    commands:
    - npm install
    - npm test
  docker:
    image: plugins/docker
    repo: quay.io/natlibfi/melinda-transliteration-ui
    auto_tag: true
    secrets: [docker_content_trust_repository_passphrase]
    environment:
    - DOCKER_ENABLE_CONTENT_TRUST=1
