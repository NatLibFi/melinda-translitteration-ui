pipeline:
  test:
    image: node:8
    commands:
    - npm install
    - npm test
  docker:
    image: quay.io/natlibfi/drone-plugin-docker:v1
    repo: quay.io/natlibfi/melinda-transliteration-ui
    tags: latest
    auth: "${QUAY_PUSH_SECRET}"
